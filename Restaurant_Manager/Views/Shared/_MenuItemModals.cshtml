<div id="deleteMenuItemModal" class="modal-overlay">
    <div class="modal-content">
        <h2>Confirm Deletion</h2>
        <p>Are you sure you want to delete this menu item?</p>

        <form id="deleteMenuItemForm" method="post">
            <input type="hidden" id="deleteMenuItemId" name="id" />
            <div class="form-actions">
                <button type="submit" class="btn shared-button-style danger">Yes, Delete</button>
                <button type="button" class="btn shared-button-style" onclick="closeDeleteModal()">Cancel</button>
            </div>
        </form>
    </div>
</div>
