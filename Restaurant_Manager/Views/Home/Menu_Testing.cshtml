<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Menu Page</title>
    <style>
        body {
          margin: 0;
          padding: 0;
          background-color: lightblue;
          font-family: Arial, sans-serif;
        }
        /* Central container styled like an A4 sheet */
        #main-container {
          width: 80%;
          max-width: 1200px;
          margin: 40px auto;
          background-color: white;
          padding: 20px;
          box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        /* Grid to display items in 2 columns */
        .menu-grid {
          display: grid;
          grid-template-columns: repeat(2, 1fr);
          gap: 20px;
        }
        /* Each menu item card */
        .menu-item {
          border: 1px solid #ccc;
          padding: 10px;
        }
        .card {
          display: flex;
          flex-direction: column;
          height: 200px; /* Adjust height as needed */
          border: 1px solid #ddd;
        }
        /* Top part for image (65% of card height) */
        .image-container {
          flex: 65;
          position: relative;
          overflow: hidden;
        }
        .image-container img {
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
        /* Price overlay on top of the image */
        .price-overlay {
          position: absolute;
          top: 0;
          left: 0;
          background: rgba(0, 0, 0, 0.6);
          color: #fff;
          padding: 5px 10px;
          font-size: 14px;
        }
        /* Bottom part for the dish name (35% of card height) */
        .dish-name {
          flex: 35;
          display: flex;
          align-items: center;
          justify-content: center;
          background: #f8f8f8;
          font-weight: bold;
        }
        /* Details section below the card */
        .details {
          margin-top: 10px;
          text-align: center;
        }
        .details p {
          margin: 5px 0;
        }
        .details button {
          padding: 8px 16px;
          background: #28a745;
          border: none;
          color: white;
          cursor: pointer;
          font-size: 14px;
        }
        .details button:hover {
          background: #218838;
        }
    </style>
</head>
<body>
    <div id="main-container">
        <div class="menu-grid">
            <!-- First menu item -->
            <div class="menu-item">
                <div class="card">
                    <div class="image-container">
                        <img src="wwwroot\Images_Menu_rm\orange_juice_menu.webp" alt="Dish Image">
                        <div class="price-overlay">Price: 10.30</div>
                    </div>
                    <div class="dish-name">Dish Name</div>
                </div>
                <div class="details">
                    <p>A delicious dish description goes here.</p>
                    <button>Order</button>
                </div>
            </div>
            <!-- Second menu item -->
            <div class="menu-item">
                <div class="card">
                    <div class="image-container">
                        <img src="https://via.placeholder.com/400x200" alt="Dish Image">
                        <div class="price-overlay">Price: 12.50</div>
                    </div>
                    <div class="dish-name">Another Dish</div>
                </div>
                <div class="details">
                    <p>A delightful dish with exquisite flavors.</p>
                    <button>Order</button>
                </div>
            </div>
            <!-- Add more .menu-item blocks as needed -->
        </div>
    </div>
</body>
</html>
